{
  "name": "offsura-firebase-streams",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "firebase-admin": "^9.5.0",
    "prettier": "^2.2.1",
    "rxjs": "^6.6.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  },
  "peerDependencies": {
    "rxjs": "^6.6.3"
  },
  "dependencies": {
    "debug": "^4.3.1"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "patch-push": "yarn build && yarn version --patch && yarn publish --access public"
  }
}
